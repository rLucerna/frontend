# 4주차 이후 학습 로드맵

> 4주차까지의 스터디를 마친 후, 자율적으로 학습하면 좋은 주제들을 정리한 문서입니다.
> 우선순위가 높은 순서대로 정리되어 있으며, 각 주제가 왜 필요한지와 언제 배우면 좋은지를 함께 안내합니다.

---

## 📌 목차

1. [React Query (TanStack Query)](#1-react-query-tanstack-query)
2. [인증(Auth) 흐름](#2-인증auth-흐름)
3. [폼 처리와 유효성 검증](#3-폼-처리와-유효성-검증)
4. [AsyncStorage / SecureStore](#4-asyncstorage--securestore)
5. [이미지 업로드와 파일 처리](#5-이미지-업로드와-파일-처리)
6. [TypeScript 심화](#6-typescript-심화)
7. [테스트 코드 작성](#7-테스트-코드-작성)
8. [앱 배포](#8-앱-배포)

---

## 1. React Query (TanStack Query)

### 왜 배워야 하나?

4주차에서 `useEffect + useState`로 API 데이터를 관리하는 패턴을 배웠습니다.
하지만 실무에서는 이 패턴으로 해결하기 어려운 상황이 많습니다:

- 같은 API를 여러 화면에서 호출할 때 **중복 요청** 문제
- 데이터를 **캐싱**해서 불필요한 재요청 방지
- 화면에 다시 돌아왔을 때 **자동으로 데이터 갱신**
- 페이지네이션, 무한 스크롤 처리

React Query는 이러한 **서버 상태(Server State)** 관리를 자동화해주는 라이브러리입니다.

### 핵심 개념

```tsx
import { useQuery } from "@tanstack/react-query";

function UserList() {
  const { data, isLoading, error } = useQuery({
    queryKey: ["users"],
    queryFn: () => axios.get("/users").then((res) => res.data),
  });
  // loading, error, data 상태를 자동 관리!
}
```

### 언제 배우면 좋은가?

4주차 내용(axios + Zustand)에 익숙해진 후. Zustand는 **클라이언트 상태**(UI 상태, 사용자 설정 등)에, React Query는 **서버 상태**(API 데이터)에 사용하는 것이 현대적인 패턴입니다.

---

## 2. 인증(Auth) 흐름

### 왜 배워야 하나?

거의 모든 앱에는 로그인/로그아웃 기능이 있습니다. 인증 흐름을 이해하면:

- 로그인 → 토큰 발급 → 저장 → API 요청 시 자동 첨부
- 토큰 만료 → 자동 갱신 또는 재로그인 유도
- 인증 여부에 따른 화면 분기 (로그인 화면 vs 메인 화면)

이 모든 과정을 처리할 수 있게 됩니다.

### 핵심 개념

- JWT(JSON Web Token)의 구조와 역할
- Access Token / Refresh Token
- 토큰 저장 위치 (SecureStore)
- axios 인터셉터로 토큰 자동 첨부
- 인증 상태에 따른 네비게이션 분기

### 언제 배우면 좋은가?

4주차에서 배운 axios 인터셉터와 Zustand를 직접 활용하는 주제이므로, 4주차 직후에 배우는 것을 추천합니다.

---

## 3. 폼 처리와 유효성 검증

### 왜 배워야 하나?

회원가입, 게시글 작성, 설정 변경 등 **사용자 입력을 받아서 서버에 보내는** 작업은 매우 빈번합니다.

- 여러 입력값을 효율적으로 관리
- 입력값이 올바른지 검증 (이메일 형식, 비밀번호 길이 등)
- 에러 메시지 표시
- 제출(submit) 처리

### 핵심 도구

- **react-hook-form**: 폼 상태 관리 라이브러리 (성능 우수)
- **yup** 또는 **zod**: 유효성 검증 스키마 라이브러리

### 언제 배우면 좋은가?

실제 앱을 만들기 시작할 때. 특히 로그인/회원가입 화면을 구현하게 되면 자연스럽게 필요해집니다.

---

## 4. AsyncStorage / SecureStore

### 왜 배워야 하나?

앱을 껐다가 다시 켜도 **데이터가 유지**되어야 하는 경우가 있습니다:

- 로그인 토큰 (앱을 다시 열어도 로그인 유지)
- 사용자 설정 (다크모드 등)
- 온보딩 완료 여부

### 핵심 개념

| 저장소 | 용도 | 보안 |
|--------|------|------|
| AsyncStorage | 일반 데이터 (설정, 캐시) | 낮음 (평문 저장) |
| SecureStore | 민감한 데이터 (토큰, 비밀번호) | 높음 (암호화 저장) |

### 언제 배우면 좋은가?

인증 흐름을 배울 때 함께 배우면 자연스럽습니다.

---

## 5. 이미지 업로드와 파일 처리

### 왜 배워야 하나?

프로필 사진 변경, 게시글에 이미지 첨부 등 이미지 처리는 앱에서 자주 필요합니다.

### 핵심 개념

- `expo-image-picker`: 갤러리/카메라에서 이미지 선택
- `FormData`: 파일을 서버에 업로드하는 형식
- `multipart/form-data`: 파일 전송용 Content-Type

### 언제 배우면 좋은가?

기본적인 API 호출에 익숙해진 후. 실제 프로젝트에서 이미지 관련 기능을 구현할 때 배우면 됩니다.

---

## 6. TypeScript 심화

### 왜 배워야 하나?

프로젝트가 커질수록 타입 안전성이 중요해집니다. API 응답 데이터의 타입을 정의하면 자동완성과 에러 방지에 큰 도움이 됩니다.

### 핵심 개념

- Interface vs Type
- Generic 타입
- API 응답 타입 정의
- 유틸리티 타입 (Partial, Pick, Omit 등)

### 언제 배우면 좋은가?

프로젝트를 진행하면서 점진적으로 학습하는 것이 가장 효과적입니다.

---

## 7. 테스트 코드 작성

### 왜 배워야 하나?

코드가 의도대로 동작하는지 자동으로 검증할 수 있으며, 리팩토링 시 기존 기능이 깨지지 않았는지 확인할 수 있습니다.

### 핵심 도구

- **Jest**: JavaScript 테스트 프레임워크
- **React Native Testing Library**: 컴포넌트 테스트

### 언제 배우면 좋은가?

기본적인 앱 개발에 익숙해진 후. 프로젝트 안정성이 중요해지는 시점에 도입하면 됩니다.

---

## 8. 앱 배포

### 왜 배워야 하나?

개발한 앱을 실제 기기에서 사용하거나 앱스토어에 출시하기 위해 필요합니다.

### 핵심 개념

- **EAS Build**: Expo의 클라우드 빌드 서비스
- **EAS Submit**: 앱스토어/플레이스토어 제출
- **OTA Update**: 앱 업데이트를 스토어 심사 없이 배포
- 앱 아이콘, 스플래시 화면 설정

### 언제 배우면 좋은가?

앱의 핵심 기능이 완성된 후.

---

## 추천 학습 순서

```
4주차 완료
    │
    ▼
[AsyncStorage / SecureStore]  ← 간단하고 바로 활용 가능
    │
    ▼
[인증(Auth) 흐름]  ← 거의 모든 앱에 필수
    │
    ▼
[폼 처리]  ← 로그인/회원가입 화면 구현 시 필요
    │
    ▼
[React Query]  ← API 호출이 복잡해질 때
    │
    ▼
[이미지 업로드]  ← 필요한 시점에
    │
    ▼
[TypeScript 심화]  ← 프로젝트 진행하면서 점진적으로
    │
    ▼
[테스트 / 배포]  ← 프로젝트 마무리 단계
```
